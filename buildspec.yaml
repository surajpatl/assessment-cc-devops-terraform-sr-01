version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm ci

  build:
    commands:
      - echo Running lint
      - npm run lint
      - echo Running tests
      - npm run test
      - echo Building
      - npm run build

artifacts:
  files:
    - '**/*'